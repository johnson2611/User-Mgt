<style>
  table {
    width: 100%;
    border-collapse: collapse;
  }
  th, td {
    border: 1px solid #ddd;
    padding: 8px;
  }
  th {
    background-color: #f2f2f2;
    text-align: left;
  }
  td {
    text-align: left;
  }
  .btn {
    margin: 0 5px;
  }
</style>




<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Email</th>
      <th>Gender</th>
      <th>Status</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach(user => { %>
    <tr>
      <td><%= user._id.substring(0, 8) %></td>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= user.gender %></td>
      <td><%= user.status %></td>
      <td>
        <a href="http://localhost:3300/update-user/<%= user._id %>" class="btn border-shadow update">
          <span class="text-gradient">
            <i class="fas fa-pencil-alt"></i>
          </span>
        </a>
        
        <button class="btn border-shadow delete" data-id="<%= user._id %>">
          <span class="text-gradient">
            <i class="fas fa-times"></i>
          </span>
        </button>
      </td>
    </tr>
    <% }); %>
  </tbody>
</table>



<!-- 
 <table>
  <tbody>
    <% users.forEach(user => { %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.gender %></td>
        <td><%= user.status %></td>
        <td>
          <a href="/users/<%= user._id %>/edit" class="btn border-shadow update">
            <span class="text-gradient">
              <i class="fas fa-pencil-alt"></i>
            </span>
          </a>
          <form method="POST" action="/api/users/<%= user._id %>?_method=DELETE" class="inline">
            <button type="submit" class="btn border-shadow delete" onclick="return confirm('Are you sure you want to delete this user?')">
              <span class="text-gradient">
                <i class="fas fa-times"></i>
              </span>
            </button>
          </form>
        </td>
      </tr>
    <% }); %>
  </tbody>
</table> -->




<!-- 
<table class="table">
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Email</th>
      <th>Gender</th>
      <th>Status</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach(user => { %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.gender %></td>
        <td><%= user.status %></td>
        <td>
          <a href="/users/<%= user._id %>/edit" class="btn border-shadow update">
            <span class="text-gradient">
              <i class="fas fa-pencil-alt"></i>
            </span>
          </a>
          <button class="btn border-shadow delete" data-id="<%= user._id %>">
            <span class="text-gradient">
              <i class="fas fa-times"></i>
            </span>
          </button>
        </td>
      </tr>
    <% }); %>
  </tbody>
</table>
